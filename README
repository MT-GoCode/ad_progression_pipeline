Expected Data:

- seven visits CSV file, with properties:

  - no NA values
  
  - irrelevant columns like ...7 removed (keep NACCID and CDRSUM)
  
  - progression NOT determined

  - values are either categorical or numerical

- a list of categorical columns

- a list of unchanging/constant columns

step 1: ingestion

INPUT:

- CSV

FUNCTIONS:

categorical_ingestion():

- splits input/output df based on # of input visits

- adds progression column based on whether output increases

- flattens input rows and appends progression column. Note that only one copy of constant columns are kept.

sequence_ingestion():

- splits input/output df based on # of input visits

- one-hot encodes categorical columns
